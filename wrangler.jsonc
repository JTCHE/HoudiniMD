{
  "$schema": "node_modules/wrangler/config-schema.json",
  "main": "worker.ts",
  "name": "houdinimd",
  // Runs before every `wrangler deploy`, so the git-push → Cloudflare Workers
  // Build path produces `.open-next/` (worker.js + assets) even though its build
  // command is only `next build`. Without this, deploy fails instantly:
  // "assets.directory .open-next/assets does not exist". The local `npm run
  // deploy` script builds first too, so this just makes wrangler self-sufficient.
  "build": {
    "command": "npx opennextjs-cloudflare build",
  },
  "compatibility_date": "2026-06-02",
  "compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS",
  },
  "observability": {
    "enabled": false,
    "head_sampling_rate": 1,
    "logs": {
      "enabled": true,
      "head_sampling_rate": 1,
      "persist": true,
      "invocation_logs": true,
    },
    "traces": {
      "enabled": true,
      "persist": true,
      "head_sampling_rate": 1,
    },
  },
  "r2_buckets": [
    {
      // R2 bucket used for ISR/SSG page cache.
      // Create with: npx wrangler r2 bucket create houdinimd-cache
      "binding": "NEXT_INC_CACHE_R2_BUCKET",
      "bucket_name": "houdinimd-cache",
    },
  ],
  // Durable Object that runs ISR revalidation in the background (off the request
  // path). Configured via `queue: doQueue` in open-next.config.ts. SQLite-backed
  // (see the migration below), which is available on the Workers free plan.
  "durable_objects": {
    "bindings": [
      {
        "name": "NEXT_CACHE_DO_QUEUE",
        "class_name": "DOQueueHandler",
      },
    ],
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["DOQueueHandler"],
    },
  ],
  "routes": [
    {
      "pattern": "houdinimd.jchd.me/*",
      "zone_name": "jchd.me",
    },
  ],
  "services": [
    {
      // Self-reference service binding, the service name must match the worker name
      // see https://opennext.js.org/cloudflare/caching
      "binding": "WORKER_SELF_REFERENCE",
      "service": "houdinimd",
    },
  ],
  "images": {
    // Enable image optimization
    // see https://opennext.js.org/cloudflare/howtos/image
    "binding": "IMAGES",
  },
}
